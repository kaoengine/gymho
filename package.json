{
  "name": "root",
  "private": true,
  "scripts": {
    "lw": "lerna-wizard",
    "test": "lerna run test --scope={@firebase/connect,@gymho/common}",
    "clean": "npm cache clean --forces && rm -rf node_modules package-lock.json",
    "start": "lerna bootstrap  && lerna run --parallel start --scope=@app/gymho",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff",
    "build": "lerna run build --scope=@app/gymho",
    "changelog": "lerna-changelog",
    "publish": "lerna publish",
    "start-app": "node src/app/dist/index.html"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
  },
  "dependencies": {
    "lerna-wizard": "^1.1.1"
  },
  "changelog": {
    "labels": {
      "feature": "New Feature",
      "bug": "Bug Fix"
    }
  }
}